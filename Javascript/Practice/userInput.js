// const readline = require('readline');

// const rl = readline.createInterface({
//   input: process.stdin,
//   output: process.stdout
// });

// rl.question('What is your favorite color? ', (answer) => {
//   // The user input is available in the 'answer' callback parameter
//   console.log(`Your favorite color is: ${answer}`);
  
//   rl.close(); // Important: closes the interface and allows the program to exit
// });

console.log(null==0);
